// Public API of the theme feature. Other code imports from '@/features/theme'
// only — never from internal files.
export { ThemeToggle } from './components/ThemeToggle'
export { useApplyTheme } from './hooks'
