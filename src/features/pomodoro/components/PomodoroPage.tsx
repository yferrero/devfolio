export default function PomodoroPage() {
  return (
    <section>
      <h1 className="text-2xl font-bold">Pomodoro</h1>
      <p className="mt-2 text-gray-600 dark:text-gray-400">
        Phase 5 — Zustand + timers: useRef, effect cleanup, stale closures.
      </p>
    </section>
  )
}
