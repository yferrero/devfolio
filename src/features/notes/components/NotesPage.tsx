export default function NotesPage() {
  return (
    <section>
      <h1 className="text-2xl font-bold">Notes</h1>
      <p className="mt-2 text-gray-600 dark:text-gray-400">
        Phase 4 — Redux Toolkit: slices, actions, selectors, immutable updates.
      </p>
    </section>
  )
}
